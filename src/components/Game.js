import React from "react";

export const Game = ({ game }) => {
  if (!game) return <h1>Loading...</h1>;

  return (
    <>
      <div className="" />
    </>
  );
};

export default Game;
